[pytest]
# Default run skips @pytest.mark.integration (opt-in: pytest -m integration).
addopts = -m "not integration"
markers =
    integration: optional tests that call live APIs (also set RUN_INTEGRATION=1)
